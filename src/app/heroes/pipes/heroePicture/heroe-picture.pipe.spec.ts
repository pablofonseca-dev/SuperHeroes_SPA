import { HeroePicturePipe } from './heroe-picture.pipe';

describe('HeroePicturePipe', () => {
  it('create an instance', () => {
    const pipe = new HeroePicturePipe();
    expect(pipe).toBeTruthy();
  });
});
